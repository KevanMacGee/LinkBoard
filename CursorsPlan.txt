LinkBoard Single-file → CSS/JS Split (no behavior change)
Files to edit/create
app/index.html (edit)
app/styles.css (new)
app/app.js (new)
app/storage.local.js (new)
Step 1: Extract CSS to styles.css
In app/index.html <head>, locate the single <style>...</style> block.
Copy all CSS from that block into app/styles.css.
Remove the <style>...</style> block and insert:
<link rel="stylesheet" href="styles.css" />
Do not change anything else in <head>.
Step 2: Extract main JS to app.js
At the very end of <body>, keep the SortableJS CDN script as-is.
Create app/app.js and move the entire inline <script>...</script> contents into it (from theme logic start to the helper functions end).
In app/index.html, replace the inline script tag with:
<script src="storage.local.js"></script>
<script src="app.js"></script>
Final bottom-of-body script order:
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.2/Sortable.min.js"></script>
<script src="storage.local.js"></script>
<script src="app.js"></script>
Step 3: Create storage.local.js (helpers only)
In the JS moved to app.js, identify and move the following definitions into app/storage.local.js:
const STORAGE_KEY = "linkboard.v1";
const INITIAL_COLUMN_COUNT = 4;
function createBlankColumns(...)
function createInitialState(...)
function load(...)
function save(...)
function ensureColumns(...) (if present)
function migrateState(...)
Wrap them in an IIFE that exposes window.LinkBoardStorage:
(function () {
  const STORAGE_KEY = "linkboard.v1";
  const INITIAL_COLUMN_COUNT = 4;

  function createBlankColumns(count = INITIAL_COLUMN_COUNT) {
    // ... original body ...
  }

  function createInitialState() {
    // ... original body ...
  }

  function load() {
    // ... original body ...
  }

  function save(state) {
    // ... original body ...
  }

  function ensureColumns(state) {
    // ... original body (if applicable) ...
  }

  function migrateState(state) {
    // ... original body ...
  }

  window.LinkBoardStorage = {
    STORAGE_KEY,
    INITIAL_COLUMN_COUNT,
    createBlankColumns,
    createInitialState,
    load,
    save,
    ensureColumns,
    migrateState,
  };
})();
Do NOT move or duplicate the state variable or its initialization here.
Step 4: Update app.js to use LinkBoardStorage (keep state here)
At the top of app/app.js (after any "use strict"), destructure helpers:
const {
  STORAGE_KEY,
  INITIAL_COLUMN_COUNT,
  createBlankColumns,
  createInitialState,
  load,
  save,
  ensureColumns,
  migrateState,
} = window.LinkBoardStorage;
Ensure the single authoritative state is initialized here (preserve original logic):
let state = load();
if (!state) state = createInitialState();
migrateState(state);
if (typeof ensureColumns === "function") ensureColumns(state);
save(state);
Leave the rest of the code unchanged; continue to use state and call save(state) where it previously called save()/save(state).
Step 5: Sanity check
index.html uses <link rel="stylesheet" href="styles.css" />.
Script order at bottom is SortableJS → storage.local.js → app.js.
Open index.html via file:// and confirm identical UI and behavior:
add/edit/delete cards, drag & drop, columns, export/import, reset
theme toggle, keyboard shortcuts, bookmarklet
Confirm localStorage key linkboard.v1 and stored shape unchanged.
Notes/Constraints
No build tooling; no DOM structure/class changes; no behavior changes.
Do not alter the SortableJS CDN script tag.
Do not introduce getState/setState yet.
Do not move state into storage.local.js.
Keep function bodies identical; only relocation and exposure via IIFE.
